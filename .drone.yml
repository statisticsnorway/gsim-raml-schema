kind: pipeline
name: default

steps:

  - name: test
    image: docker:stable-dind
    privileged: true
    commands:
      - sleep 5
      - docker ps
      - docker run -v $(pwd):/raml-project statisticsnorway/raml-to-jsonschema
    when:
      event:
        exclude:
        - pull_request
      branch:
      - devtest
    volumes:
      - name: docker_run
        path: /var/run/
      - name: docker_lib
        path:  /var/lib/docker
